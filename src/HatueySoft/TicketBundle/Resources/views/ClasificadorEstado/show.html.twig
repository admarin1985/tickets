{% extends '@HatueySoftTicket/layout.html.twig' %}

{% block title 'ClasificadorEstado' %}

{% block tituloPagina %}
    <h1>ClasificadorEstado</h1>
{% endblock %}

{% block content -%}
    <h1>ClasificadorEstado</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ entity.estado }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('clasificadorestado') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('clasificadorestado_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
