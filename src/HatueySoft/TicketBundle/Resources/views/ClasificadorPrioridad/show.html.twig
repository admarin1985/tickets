{% extends '@HatueySoftTicket/layout.html.twig' %}

{% block title 'ClasificadorPrioridad show' %}

{% block tituloPagina %}
    <h1>ClasificadorPrioridad show</h1>
{% endblock %}

{% block content -%}

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.nombre }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('clasificadorprioridad') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('clasificadorprioridad_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
