{% extends '@HatueySoftTicket/layout.html.twig' %}

{% block title 'ClasificadorTicket show' %}

{% block tituloPagina %}
    <h1>ClasificadorTicket show</h1>
{% endblock %}

{% block content -%}

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.nombre }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('clasificadorticket') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('clasificadorticket_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
